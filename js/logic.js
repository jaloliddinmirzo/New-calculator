import {
    button, label,
    amal,
    opertor,
    del,
    reset,
    equel
} from "./ui.js";
export function buttonFunc(item) {
    label.focus()
    label.value += item.textContent
    if (tru) {
        b = parseFloat(label.value)
    }

}
